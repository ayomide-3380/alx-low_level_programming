#include "main.h"

/**
 * void print_numbers(void) - print numbers
 * Return: Always (success)
 */
void print_numbers(void)
{

	int i;

	for (i = 0; i < 10; i++)
		_putchar("%d", i);
	_putchar("\n");

  return (i);

}