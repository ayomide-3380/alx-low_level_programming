#include "main.h"

/**
 * _abs - function that computes absolute value of an integer
 * @n: compute value of the argument
 * Return: Always (success)
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
